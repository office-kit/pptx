---
"@office-kit/pptx-dev": patch
---

Create and edit common charts from English and Japanese preview dialogs.
Change chart types, titles, series colors, category labels and numeric data,
add or remove rows and series, and preserve edits through undo and reload.
Keep embedded Excel data synchronized with the saved chart.
