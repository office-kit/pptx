---
"@office-kit/pptx": minor
"@office-kit/pptx-dev": patch
---

Preserve mixed text formatting during inline preview edits. `setShapeText` now accepts `preserveFormatting` for incremental changes, retaining unchanged runs, paragraph properties, fields and hyperlinks.
