---
"@office-kit/pptx-dev": patch
---

Add an English/Japanese aspect-ratio lock for numeric shape sizing in the preview. Update paired dimensions in one undo step while preserving position and rotation, and reject proportional sizes outside the document format's limits.
