---
'@office-kit/pptx-dev': patch
---

Read stored end-of-paragraph formatting when the caret is in an empty paragraph, so inline formatting indicators and toggles use the intended typing style in text boxes and table cells.
