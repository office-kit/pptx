---
'@office-kit/pptx-dev': minor
---

Run multiple coding agents in resizable horizontal or vertical panes, each with its own session and the focused slide as context. Refresh the preview with an Office Kit logo, dark workspace chrome, violet accents and a softly lit slide canvas. Fix terminal sizing so Claude Code's rightmost characters remain visible when resizing or splitting panes.
