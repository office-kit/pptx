---
'@office-kit/pptx': minor
'@office-kit/pptx-dev': minor
---

Allow `setShapeZIndex` to insert an ordered batch of sibling shapes in one update, preserving non-shape XML and rejecting mixed containers before mutation.

Add a layer preview for Arrange > Reorder Overlapping Objects. Drag or use arrow keys to stage an order, cancel without changing the document, or confirm with one undoable change.
