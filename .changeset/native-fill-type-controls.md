---
"@office-kit/pptx-dev": patch
---
Switch between no fill, solid fill, and gradient fill directly in the Format Shape pane. Show the selected fill's controls, restore its previous settings when switching back, and undo a fill-type change in one step. New gradients use PowerPoint's four-stop defaults.
