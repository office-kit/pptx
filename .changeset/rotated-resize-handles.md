---
"@office-kit/pptx-dev": patch
---

Keep rotated objects' resize handles aligned with the pointer and preserve the opposite anchor, including at minimum size. Hold Shift to maintain aspect ratio. Resize and rotation handles now have Japanese accessible labels.
