---
'pptx-kit-site': patch
---

site(playground): show per-slide `table` and `link` badges alongside
the existing `chart` / `media` ones. Surfaces the slide-level table
count and shape-level hyperlink count at a glance — the same data the
new `getSlideTables` / `findShapesWithHyperlinks` helpers expose
programmatically.
