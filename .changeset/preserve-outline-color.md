---
"@office-kit/pptx": patch
"@office-kit/pptx-dev": patch
---

Keep existing outline color, theme references, and opacity when changing only line width. Image border width and dash controls preserve the existing color instead of replacing it with an opaque resolved color.
