---
"@office-kit/pptx-dev": patch
---

Preserve untouched coordinate and size precision when editing one geometry field in the preview. Reject empty, negative-size and out-of-range values before mutation, restore invalid inputs, and support fractional positions and rotations with an accessible bilingual rotation label.
