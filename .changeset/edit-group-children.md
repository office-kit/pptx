---
"@office-kit/pptx": minor
"@office-kit/pptx-dev": patch
---

Edit individual objects inside groups in the development preview, with English and Japanese group navigation, transformed pointer/keyboard movement, and undoable deletion and duplication.

Allow `copyShape` to preserve ancestor group transforms with `preserveGroupTransform`. Deletion and z-order operations now handle nested shapes in their owning group and preserve trailing extension metadata.
