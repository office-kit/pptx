---
'@office-kit/pptx-dev': minor
---

Import common inline character styles when pasting HTML text into shapes or table cells, and include HTML formatting when copying text to other applications. Clipboard markup stays inert; unrecognized text layouts fall back to plain text.
