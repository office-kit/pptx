---
"@office-kit/pptx": minor
"@office-kit/pptx-dev": patch
---

Add a Background Styles gallery to the editor's Design ribbon, with theme color previews, master-wide application, undo, and links to background formatting and reset. Expose `getSlideMasterBackgroundStyles` to read the available presets and master selection without modifying a presentation.
