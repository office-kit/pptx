---
"@office-kit/pptx-dev": patch
---

Edit matching gradient stops across selected shapes in one undo step while retaining each shape's angle, scaling and rotation settings. Show mixed angle and rotation values in the format pane.
