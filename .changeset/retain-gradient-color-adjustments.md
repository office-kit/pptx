---
"@office-kit/pptx": minor
"@office-kit/pptx-dev": patch
---

Preserve imported gradient stop color adjustments when changing gradient position, direction, brightness, or transparency in the editor. Gradient stops now expose ordered `colorTransforms` for round-trip editing through the API.
