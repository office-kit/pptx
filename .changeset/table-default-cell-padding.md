---
"@office-kit/pptx-preview": patch
---

Correct default table cell margins in SVG and browser previews so text wrapping and padding remain proportional when fitting content to a new page size. Explicit zero margins remain supported.
